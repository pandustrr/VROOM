import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 20,
        width: '100%',
    },
    judul: {
        fontSize: 24,
        fontWeight: 'bold',
        marginBottom: 20,
        textAlign: 'center',
    },
    input: {
        borderWidth: 1,
        borderColor: '#ccc',
        borderRadius: 5,
        padding: 10,
        marginBottom: 15,
        backgroundColor: '#fff',
    },
    link: {
        color: '#007bff',
        marginTop: 10,
        textAlign: 'center',
    },
    message: {
        color: 'red',
        textAlign: 'center',
        marginVertical: 10,
    }
});

export default styles;
